import { useState } from 'react';
import viteLogo from '/logo.svg';
import './App.css';

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>Movies</div>
  );
}

export default App;
